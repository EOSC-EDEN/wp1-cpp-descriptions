name: No validation required

on:
    workflow_dispatch:
    pull_request:
        branches:
            - main
        paths-ignore:
            - 'CPP-*/cpp-*.xml'
            - 'cpp.xsd'

jobs:
    validate:
        runs-on: ubuntu-latest
        steps:
            - name: Nothing to do
              run: echo "CPPs not modified; still valid"
